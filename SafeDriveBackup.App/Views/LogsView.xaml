<UserControl x:Class="SafeDriveBackup.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="Segoe UI">

    <Grid Background="{StaticResource ContentBg}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header bar -->
        <Border Grid.Row="0" Padding="28,20,28,16"
                Background="White" BorderBrush="{StaticResource BorderColor}"
                BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Style="{StaticResource H1}" Text="Backup Logs"/>
                    <TextBlock Style="{StaticResource Muted}" Margin="0,4,0,0"
                               Text="View the log files written during each backup session."/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <ComboBox ItemsSource="{Binding LogFiles}"
                              SelectedItem="{Binding SelectedLogFile}"
                              MinWidth="200" Margin="0,0,12,0"
                              FontSize="13"/>
                    <Button Content="Refresh"
                            Style="{StaticResource SecondaryBtn}"
                            Command="{Binding RefreshCommand}"
                            Margin="0,0,8,0"/>
                    <Button Content="Open Folder"
                            Style="{StaticResource SecondaryBtn}"
                            Command="{Binding OpenFolderCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Log content -->
        <Border Grid.Row="1" Margin="28,16,28,28" Style="{StaticResource Card}" Padding="0">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Auto"
                          x:Name="LogScroller">
                <TextBlock Text="{Binding LogContent}"
                           FontFamily="Consolas, Courier New"
                           FontSize="12"
                           Foreground="{StaticResource TextPrimary}"
                           Padding="16"
                           TextWrapping="NoWrap"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
